# Runtime dependencies — mirrors [project.dependencies] in pyproject.toml.
# Preferred install is the editable package:  pip install -e ".[dev]"
# This file exists for environments that want a plain  pip install -r  instead.

beautifulsoup4
httpx>=0.27
trafilatura>=1.12
playwright>=1.40
psycopg[binary]>=3.2
pydantic>=2.7
pydantic-settings>=2.6
python-dotenv
requests

# Test dependencies (the [dev] extra):
# pytest>=8.0
# pytest-asyncio>=0.24

# Insight distillation (the [enrich] extra), for
# scripts/enrichment/distill_insights_to_db.py:
# pgvector>=0.4
# google-genai>=2.8
# openai>=2.0
# tiktoken
# The regression model (the [model] extra), for model_shay/. Prefer
# pip install -e ".[model]" so the CUDA wheel index can be pointed at the
# local GPU (sm_120 needs cu128); see the comment above the extra in
# pyproject.toml.
# torch>=2.7
# transformers>=4.44
# accelerate>=0.34
# peft>=0.13
# sentencepiece>=0.2
# protobuf>=4.25
# scikit-learn>=1.5
# pandas>=2.2
# numpy>=1.26
# pyarrow>=17
# pyyaml>=6.0
#
# Extended gain features (also the [enrich] extra), for
# scripts/enrichment/add_extended_gain_features.py:
# yfinance>=0.2
