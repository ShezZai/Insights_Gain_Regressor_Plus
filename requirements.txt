# Runtime dependencies — mirrors [project.dependencies] in pyproject.toml.
# Preferred install is the editable package:  pip install -e ".[dev]"
# This file exists for environments that want a plain  pip install -r  instead.

beautifulsoup4
httpx>=0.27
trafilatura>=1.12
playwright>=1.40
psycopg[binary]>=3.2
pydantic>=2.7
pydantic-settings>=2.6
python-dotenv
requests

# Test dependencies (the [dev] extra):
# pytest>=8.0
# pytest-asyncio>=0.24

# Insight distillation (the [enrich] extra), for
# scripts/enrichment/distill_insights_to_db.py:
# pgvector>=0.4
# google-genai>=2.8
# openai>=2.0
# tiktoken
